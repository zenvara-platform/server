# This repo holds releases, issues, and discussions — not source code.
# Keep it free of downloaded binaries and local cruft.

# Release artifacts (distributed via GitHub Releases, never committed)
*.zip
*.tar
*.tar.gz
*.tgz
*.7z
*.exe
*.msi
*.dmg
*.pkg
*.deb
*.rpm
*.AppImage
*.sha256
*.sig

# Logs
*.log
